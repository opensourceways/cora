name: "forum/groups-get"
skip: true
skip_reason: "Forum instance IP whitelist not configured for CI runner — enable when network access is granted"
service: forum
args:
  - "groups"
  - "get"
  - "--name"
  - "staff"
format: json
timeout_ms: 10000
assertions:
  - type: exit_code
    value: 0
  - type: json_has_keys
    values: ["group"]
  - type: json_key_not_empty
    key: "group"
