name: "gitcode/users-list-user"
service: gitcode
args:
  - users
  - list-user
format: json
timeout_ms: 8000
assertions:
  - type: exit_code
    value: 0
  - type: json_has_keys
    values: ["login", "name", "id", "email"]
  - type: json_key_not_empty
    key: "login"
