name: "forum/users-get-email"
service: forum
args:
  - "users"
  - "get-email"
  - "--username"
  - "system"
format: json
timeout_ms: 10000
assertions:
  - type: exit_code
    value: 0
  - type: json_has_keys
    values: ["email"]
  - type: json_key_not_empty
    key: "email"
