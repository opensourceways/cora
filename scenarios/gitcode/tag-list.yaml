name: "gitcode/tag-list"
service: gitcode
args:
  - tag
  - list
  - --owner
  - openeuler
  - --repo
  - infrastructure
format: table
timeout_ms: 8000
assertions:
  - type: exit_code
    value: 0
  - type: stdout_not_empty
