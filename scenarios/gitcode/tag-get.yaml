name: "gitcode/tag-get"
service: gitcode
skip: true
skip_reason: "Protected tag API requires admin permissions"
args:
  - tag
  - list
  - --owner
  - openeuler
  - --repo
  - infrastructure
format: table
timeout_ms: 8000
assertions:
  - type: exit_code
    value: 0
