name: "Jenkins · jobs list"
service: jenkins
args:
  - jobs
  - list
format: table
timeout_ms: 10000
skip: true
skip_reason: "Requires configured Jenkins instance"
assertions:
  - type: exit_code
    value: 0
  - type: stdout_not_empty
  - type: stderr_not_contains
    value: "ERROR"
