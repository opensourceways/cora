name: "github/users-get"
service: github
args:
  - users
  - get
  - --username
  - TommyLike
format: json
timeout_ms: 10000
assertions:
  - type: exit_code
    value: 0
  - type: json_has_keys
    values: ["login", "name", "id", "html_url"]
  - type: json_key_not_empty
    key: "login"
