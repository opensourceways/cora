name: "github/users-list-user"
service: github
args:
  - users
  - list-user
format: json
timeout_ms: 10000
assertions:
  - type: exit_code
    value: 0
  - type: json_has_keys
    values: ["login", "name", "id"]
  - type: json_key_not_empty
    key: "login"
