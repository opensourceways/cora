name: "gitcode/release-list"
service: gitcode
args:
  - release
  - list
  - --owner
  - openeuler
  - --repo
  - infrastructure
format: json
timeout_ms: 8000
assertions:
  - type: exit_code
    value: 0
