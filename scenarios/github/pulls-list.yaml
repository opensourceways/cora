name: "github/pulls-list"
service: github
args:
  - pulls
  - list
  - --owner
  - opensourceways
  - --repo
  - cora
  - --state
  - open
format: table
timeout_ms: 10000
assertions:
  - type: exit_code
    value: 0
  - type: stdout_not_empty
  - type: stderr_not_contains
    value: "ERROR"
