name: "forum/users-get"
service: forum
args:
  - "users"
  - "get"
  - "--username"
  - "system"
format: json
timeout_ms: 10000
assertions:
  - type: exit_code
    value: 0
  - type: json_has_keys
    values: ["user"]
  - type: json_key_not_empty
    key: "user"
