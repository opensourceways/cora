name: "forum/groups-get"
service: forum
args:
  - "groups"
  - "get"
  - "--name"
  - "staff"
format: json
timeout_ms: 10000
assertions:
  - type: exit_code
    value: 0
  - type: json_has_keys
    values: ["group"]
  - type: json_key_not_empty
    key: "group"
